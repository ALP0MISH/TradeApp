<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TradeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    
    
    <style name="Theme.TradeApp" parent="Base.Theme.TradeApp">
        <item name="android:statusBarColor">@color/black</item>
        <item name="theme_text_color">@color/white</item>
        <item name="theme_search_view_style">@color/dark_blue</item>
        <item name="theme_background">@color/black</item>
        <item name="theme_icon_background">@color/black</item>

    </style>
</resources>